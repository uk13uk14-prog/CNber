# CNber 缺陷分流与优先级指南（验收测试支持）

> **目的**：在验收或执行 `TEST_EXECUTION_LOG.md` 时，快速判断问题属于哪一类、如何写备注、如何定优先级，减少「说不清是环境问题还是 bug」的返工。

---

## 一、如何区分四类问题

### 1. 代码 bug（逻辑 / 接口 / 前端状态错误）

**特征（满足多条更像 bug）**：

- 在**正确配置、正确数据**下，行为与 `TEST_FLOW_CHECKLIST.md` 或产品约定**稳定不一致**。
- 同一操作在不同端表现**与同一后端事实矛盾**（例如 Web 显示 `assigned`，乘客端长期停在错误页且接口返回已是 `assigned`）。
- 控制台 / 网络面板有**未处理异常**、明显错误的分支（如 200 但 `code !== 0` 仍当成功）。
- 可**稳定复现**的步骤路径，不依赖「刚好这条数据很脏」。

**记录建议**：写清「步骤 → 接口与响应摘要 → 界面表现 → 期望」；附上订单 `_id`、时间、角色。

---

### 2. 数据问题（脏数据、历史数据、手工改库）

**特征**：

- 仅**个别订单 / 个别用户**异常，换一条新数据复测**正常**。
- 与**曾手工改 Mongo**、导入脚本、旧版本写入的字段有关（如状态与 `driverId` 组合非法）。
- 管理端或脚本**绕过状态机**改状态，导致前端按正常状态机推断时出现「合理但不一致」。

**记录建议**：标明异常订单 `_id`、当前 `status` / `driverId` 快照；说明是否可清洗数据后复测通过。

---

### 3. 配置问题（环境、URL、Token、跨域、未启动服务）

**特征**：

- 换一台机器 / 换 `.env` / 换 `BASE_URL` 后**现象消失或变化**。
- 网络请求为 **401 / 403 / 404 / 连接失败**，而代码路径未变。
- 多端指向**不同后端**或一端仍指向旧域名。
- **未登录**或 token 过期导致整类接口失败，被误认为「列表为空」。

**记录建议**：写明 `BASE_URL`、是否 HTTPS、是否带 `Authorization`、浏览器/真机网络环境。

---

### 4. 体验优化（非错误，属改进项）

**特征**：

- 功能与清单**一致且通过**，但「慢、丑、多一步、文案不友好」等。
- 无错误码、无逻辑错误，纯 **UX / 性能 / 文案** 诉求。

**记录建议**：单独列为「优化 backlog」，避免与缺陷混在同一优先级队列里 unless 影响验收签字。

---

## 二、如何定优先级（P0 / P1 / P2）

### P0 — 阻塞验收或阻塞核心交易

**典型**：

- 标准下单链**无法走通**（创建订单失败、指派后状态错乱导致无法继续）。
- 支付/资金安全相关（若已接入）出现**错误扣款或重复扣款**风险。
- **大面积不可用**（登录全挂、接口 5xx 持续、数据库不可写）。
- **数据损坏或泄露**类安全问题。

**行动**：停止按清单继续扩散测试，先修或先切环境；`TEST_EXECUTION_LOG` 中标记**阻塞**。

---

### P1 — 核心路径可走，但有明确缺陷或高风险边缘

**典型**：

- 主路径能完成，但 **cancelled / completed** 与清单约定不一致（如取消仍进完成页）。
- 评价链在「接口未接」时仍出现**虚假成功**（若发生即 P0/P1 视影响面定，一般至少 P1）。
- 司机端与 Web **同一订单状态展示长期不一致**且可稳定复现。
- 仅某浏览器或某端复现的**功能性错误**。

**行动**：记入执行日志与工单，本轮验收可「带缺陷签字」需业务同意。

---

### P2 — 非主路径、低频、纯体验或可绕过

**典型**：

- 文案、间距、加载动画、**可选页**（如捐赠说明）问题。
- 时间线脚注理解成本、**仅影响阅读**的展示顺序。
- 需极端操作序列才复现的小问题。

**行动**： backlog 排期，不阻塞本轮核心清单闭环。

---

## 三、分流决策简表（现场速查）

| 先问一句 | 若答案为「是」 | 倾向归类 |
|----------|----------------|----------|
| 换干净新订单 / 新账号后仍复现？ | 是 | 代码 bug 或配置（再看接口是否 4xx/5xx） |
| 仅老数据或改过库的订单？ | 是 | 数据问题 |
| 改 API 地址或重登后正常？ | 是 | 配置问题 |
| 行为符合清单，只是不爽？ | 是 | 体验优化 |

---

## 四、与测试文档的配合

- **步骤与预期**：以 `TEST_FLOW_CHECKLIST.md` 为准。  
- **填写实际结果与通过与否**：以 `TEST_EXECUTION_LOG.md` 为准。  
- **缺陷定性、优先级**：以本文为准，并在执行日志第七节挂工单链接。

---

## 五、修订

本文档仅描述验收阶段分流惯例，不替代项目级 SLA 或安全响应流程；若组织另有严重级别定义，以组织规范为准。
