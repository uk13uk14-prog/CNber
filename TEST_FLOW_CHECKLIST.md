# CNber 全链路验收测试检查清单

本文档用于**正式全链路验收**：覆盖乘客端（`CNber_client_admin_v1.0`）、管理 Web（`CNber_admin_web_v1.0`）、司机端（`CNber_driver_admin_v1.0`）与后端（`CNber_backend`）。每一步均写出**预期结果**，便于勾选记录。

---

## 前置条件

- [ ] 后端已启动，数据库可写。
- [ ] 已准备三类账号：乘客（user）、司机（driver）、管理员（admin）。
- [ ] 乘客端、Web、司机端均已配置正确的 API 基地址并可登录。

---

## 1. 标准下单链路测试步骤

| # | 操作步骤 | 预期结果 |
|---|----------|----------|
| 1.1 | 乘客登录，填写行程并提交下单（任一下单入口进入创建订单） | 接口 `POST /api/order/create` 成功，订单状态为 `pending`。 |
| 1.2 | 乘客进入等待司机页（A0107） | 展示「待接单」类文案；轮询 `GET /api/order/list` 无报错；不出现 mock 假状态。 |
| 1.3 | Web 管理端打开订单列表，找到该订单 | 状态为「待接单」；可进入订单详情。 |
| 1.4 | Web 对订单执行「分配司机」 | 订单变为 `assigned`，已关联目标司机；乘客端轮询后仍停留在等待页（A0107），文案变为「已分配司机，等待司机确认」类含义。 |
| 1.5 | 司机端登录对应司机，在列表/详情中看到 `assigned` 订单并执行接单 | `POST /api/order/accept` 成功，订单 `accepted`。 |
| 1.6 | 乘客端轮询 | 自动跳转司机信息页（A0109），可看到司机电话等真实 populate 字段。 |
| 1.7 | 司机端发起「开始行程」 | 订单 `started`；乘客端进入行程中页（A0110）。 |
| 1.8 | 司机端发起「完成订单」 | 订单 `completed`；乘客端进入行程完成页（A0111），状态为已完成。 |

---

## 2. 指派 / 撤销指派测试步骤

| # | 操作步骤 | 预期结果 |
|---|----------|----------|
| 2.1 | 对 `pending` 订单在 Web 执行分配司机 | 订单 `assigned`，`driverId` 正确。 |
| 2.2 | 在 Web 订单详情对 `assigned` 执行「撤销指派」 | 订单回到 `pending`，司机清空或符合后端规则；司机端不再显示该单为待本人确认（以实际查询为准）。 |
| 2.3 | 再次分配同一或其他司机 | 可再次变为 `assigned`，乘客端文案与轮询正常。 |

---

## 3. 抢单兼容测试步骤

| # | 操作步骤 | 预期结果 |
|---|----------|----------|
| 3.1 | 保持订单为 `pending` 且**未**在 Web 指派 | 目标司机在司机端从待接单池对该单执行接单。 |
| 3.2 | 检查订单状态与 driverId | `POST /api/order/accept` 将 `pending` 抢单为 `accepted` 并写入司机；乘客端应进入 A0109。 |
| 3.3 | 另一司机尝试接同一单 | 失败或提示已被接（与后端实现一致）。 |

---

## 4. 取消单测试步骤

| # | 操作步骤 | 预期结果 |
|---|----------|----------|
| 4.1 | 通过管理端将订单置为 `cancelled`（或业务允许的取消路径） | 订单 `cancelled`；Web 详情状态为「已取消」。 |
| 4.2 | 乘客在 A0107 / A0109 / A0110 等行程相关页等待轮询 | 若当前「主单」为最近终态且为 **cancelled**，应 **redirect 到订单历史 A0202**，**不进入** A0111 完成页。 |
| 4.3 | 乘客打开 A0202 | 列表中该单状态为「已取消」（与 `orderStatus` 文案一致）。 |
| 4.4 | 乘客从 A0111 点击「评价」入口 | 仅已完成单可带 `orderId` 进入评价页；已取消单不应满足评价页「已完成」校验（应提示不可评价）。 |

---

## 5. 完成单 / 评价测试步骤

| # | 操作步骤 | 预期结果 |
|---|----------|----------|
| 5.1 | 将一单走至 `completed`，乘客停留在 A0111 | 展示完成文案与订单摘要；「评价司机」可点击。 |
| 5.2 | 点击「评价司机」 | 跳转 A0201 且 URL 携带 `orderId`；页面拉取 `GET /api/order/detail/:id` 成功且状态为 `completed` 时允许填写评分。 |
| 5.3 | 选择星级后点击提交（不选打赏） | 当前后端**未默认开通**评价写入接口时：应弹出**明确说明接口未接入**，**不得**出现无后端支撑的「评价成功」toast。 |
| 5.4 | 选择打赏金额后点击提交 | 提示打赏/支付链未接入，阻止提交；不跳转虚假支付成功。 |
| 5.5 | 将 `CNber_client_admin_v1.0/utils/orderApi.js` 中 `ORDER_RATING_API_ENABLED` 置为 `true` 且后端实现 `POST /api/order/rating`（或约定路径）后重复 5.3 | 应真实写入并由接口返回决定成功或失败提示。 |

---

## 6. Web 订单详情时间线（可选回归）

| # | 操作步骤 | 预期结果 |
|---|----------|----------|
| 6.1 | 打开任意订单详情 | 可见「订单时间线」：创建时间使用 `createdAt`；各节点说明与 `updatedAt` 脚注与实现一致，无伪造的独立指派/接单时间。 |
| 6.2 | 若存在跟进备注 | 「备注时间线」按时间列出真实备注内容与时间。 |

---

## 7. 记录模板（现场可复制）

```
日期：_______  执行人：_______  环境：_______

用例 1.1  [ ] 通过  [ ] 失败  备注：___________
用例 1.2  [ ] 通过  [ ] 失败  备注：___________
…
```

---

## 修订说明

- 本清单随版本迭代更新；评价、打赏、支付等与后端契约以后端路由与 `ORDER_RATING_API_ENABLED` 为准。
- `cancelled` 规则：**不进入完成页 A0111**；行程相关页轮询发现主单为 `cancelled` 时优先跳转 **A0202**。
