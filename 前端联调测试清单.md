# CNber 前端联调测试清单

本文档用于当前项目的前后端联调测试，覆盖客户端与司机端核心主链路。

联调前准备：

- 启动后端：`CNber_backend`
- 确认后端地址：`http://localhost:3100/api`
- 确认 MongoDB 已启动
- 客户端项目：`CNber_client_admin_v1.0`
- 司机端项目：`CNber_driver_admin_v1.0`

---

## 客户端测试清单

### 1. 注册

- [ ] 进入页面：`A0002_client_login_v01` -> 点击 `注册账号`
- [ ] 实际跳转页面：`A0003_client_register_v01`
- [ ] 操作：
  - 输入手机号
  - 输入图形验证码
  - 输入短信验证码
  - 输入密码
  - 再次输入密码
  - 勾选协议
  - 点击 `注册`
- [ ] 预期结果：
  - 页面提示 `注册成功`
  - 自动跳转到登录页 `A0002_client_login_v01`
- [ ] 失败时如何判断问题：
  - 前端问题判断：
    - 点击 `注册` 没有反应
    - 页面未发请求
    - 输入校验异常，比如明明填了内容却仍提示“请填写完整信息”
  - 后端问题判断：
    - 页面提示 `注册失败`
    - 已发出请求，但返回 4xx/5xx
    - 返回信息如 `该手机号已注册`、`服务器内部错误`

### 2. 登录

- [ ] 进入页面：`A0002_client_login_v01`
- [ ] 操作：
  - 输入手机号
  - 输入图形验证码
  - 输入密码
  - 勾选协议
  - 点击 `登录`
- [ ] 预期结果：
  - 请求 `POST /api/auth/login`
  - 本地成功写入 `token`
  - 本地成功写入 `user`
  - 跳转到首页 `A0300_client_main_v01`
- [ ] 失败时如何判断问题：
  - 前端问题判断：
    - 登录后未跳转
    - 登录成功但未写入 `token`
    - 明明返回成功，页面仍提示失败
  - 后端问题判断：
    - 返回 `用户不存在`
    - 返回 `密码错误`
    - 返回中没有 `token`

### 3. 创建订单

- [ ] 进入页面：`A0300_client_main_v01` -> 点击 `下单中心`
- [ ] 进入路径：`A0101_client_order_service_type_v01` -> 点击 `点对点`
- [ ] 实际进入页面：`A0104_client_order_point_v01`
- [ ] 操作：
  - 输入 `起点`
  - 输入 `终点`
  - 点击 `下单`
- [ ] 预期结果：
  - 请求 `POST /api/order/create`
  - 请求头带 `Authorization: Bearer token`
  - 页面提示 `下单成功`
  - 自动跳转到订单状态页 `A0107_client_wait_driver_v01`
- [ ] 失败时如何判断问题：
  - 前端问题判断：
    - 点击按钮无请求
    - token 未带上
    - 跳转路径错误
  - 后端问题判断：
    - 返回 `未提供 token`
    - 返回 `token 无效或已过期`
    - 返回 `字段缺失`

### 4. 查看订单状态变化

- [ ] 进入页面：`A0107_client_wait_driver_v01`
- [ ] 页面行为：
  - 每 5 秒轮询一次 `GET /api/order/list`
  - 页面展示：
    - `pickup`
    - `destination`
    - `status`
    - `driverId`
- [ ] 状态联动预期：
  - `pending`：显示 `等待接单`
  - `accepted`：自动跳转 `A0109_client_driver_info_v01`
  - `ongoing`：自动跳转 `A0110_client_in_trip_v01`
  - `completed`：自动跳转 `A0111_client_trip_completed_v01`
- [ ] 如果 `driverId` 已存在：
  - 页面显示司机信息模块
- [ ] 失败时如何判断问题：
  - 前端问题判断：
    - 页面不轮询
    - 状态变化后不跳页
    - 跳页反复触发，出现重复跳转
    - 页面仍显示静态旧数据
  - 后端问题判断：
    - `/api/order/list` 没返回订单
    - 返回状态未更新
    - 返回里没有 `driverId`

---

## 司机端测试清单

### 5. 查看订单列表

- [ ] 进入页面：`D0300_driver_main` -> 点击 `接单中心`
- [ ] 实际进入页面：`D0101_driver_order_list`
- [ ] 页面行为：
  - 自动请求 `GET /api/order/list`
  - 请求头带 `Authorization: Bearer token`
- [ ] 预期结果：
  - 能看到订单列表
  - 每条订单显示：
    - `pickup`
    - `destination`
    - `status`
- [ ] 司机端返回规则预期：
  - 所有 `pending`
  - 当前司机自己的 `accepted`
  - 当前司机自己的 `ongoing`
- [ ] 失败时如何判断问题：
  - 前端问题判断：
    - 页面不发请求
    - 请求未带 token
    - 接口有数据但页面没渲染
  - 后端问题判断：
    - 返回空列表，但数据库里明明有订单
    - 司机看不到自己已接单或进行中的订单

### 6. 接单

- [ ] 进入页面：`D0101_driver_order_list`
- [ ] 操作：
  - 找到 `status = pending` 的订单
  - 点击 `接单`
- [ ] 预期结果：
  - 请求 `POST /api/order/accept`
  - 请求体包含 `orderId`
  - 请求头带 `Authorization: Bearer token`
  - 页面提示 `接单成功`
  - 自动刷新列表
  - 该订单状态从 `pending` 变为 `accepted`
- [ ] 失败时如何判断问题：
  - 前端问题判断：
    - 点击按钮无请求
    - 成功后不刷新列表
  - 后端问题判断：
    - 返回 `只有司机可以接单`
    - 返回 `订单不存在或不可接单`

### 7. 开始行程

- [ ] 进入页面：`D0101_driver_order_list`
- [ ] 操作：
  - 找到 `status = accepted` 的订单
  - 点击 `开始行程`
- [ ] 预期结果：
  - 请求 `POST /api/order/start`
  - 请求体包含 `orderId`
  - 请求头带 `Authorization: Bearer token`
  - 页面提示 `行程已开始`
  - 自动刷新列表
  - 该订单状态变为 `ongoing`
- [ ] 失败时如何判断问题：
  - 前端问题判断：
    - `accepted` 状态没显示按钮
    - 点击后没有刷新
  - 后端问题判断：
    - 返回 `只有司机可以开始行程`
    - 返回 `订单不存在或无权限开始行程`
    - 说明当前司机不是接单司机，或订单状态不对

### 8. 完成订单

- [ ] 进入页面：`D0101_driver_order_list`
- [ ] 操作：
  - 找到 `status = ongoing` 的订单
  - 点击 `完成订单`
- [ ] 预期结果：
  - 请求 `POST /api/order/complete`
  - 请求体包含 `orderId`
  - 请求头带 `Authorization: Bearer token`
  - 页面提示 `订单已完成`
  - 自动刷新列表
  - 该订单状态变为 `completed`
- [ ] 失败时如何判断问题：
  - 前端问题判断：
    - `ongoing` 状态没显示按钮
    - 完成后列表没刷新
  - 后端问题判断：
    - 返回 `只有司机可以完成行程`
    - 返回 `订单不存在或无权限完成行程`

---

## 客户端与司机端联动核验

### 9. 接单后的客户端联动

- [ ] 客户端保持停留在订单状态相关页面
- [ ] 司机完成 `接单`
- [ ] 预期结果：
  - 客户端轮询后状态从 `pending` 变为 `accepted`
  - 客户端自动跳转到 `A0109_client_driver_info_v01`
  - 页面出现司机信息展示
- [ ] 失败时如何判断问题：
  - 前端问题判断：
    - 客户端不跳页
    - 已有 `driverId` 但页面不显示司机信息
  - 后端问题判断：
    - 用户订单接口仍返回 `pending`
    - 订单未写入 `driverId`

### 10. 开始行程后的客户端联动

- [ ] 司机点击 `开始行程`
- [ ] 预期结果：
  - 客户端状态更新为 `ongoing`
  - 客户端自动跳转到 `A0110_client_in_trip_v01`
- [ ] 失败时如何判断问题：
  - 前端问题判断：
    - 客户端不跳转
  - 后端问题判断：
    - 用户订单接口仍返回 `accepted`

### 11. 完成订单后的客户端联动

- [ ] 司机点击 `完成订单`
- [ ] 预期结果：
  - 客户端状态更新为 `completed`
  - 客户端自动跳转到 `A0111_client_trip_completed_v01`
- [ ] 失败时如何判断问题：
  - 前端问题判断：
    - 客户端停留在旧页面
    - 重复跳转或页面闪烁
  - 后端问题判断：
    - 用户订单接口状态未更新成 `completed`

---

## 快速排查建议

### 更偏前端问题的典型表现

- 页面按钮点击没有任何请求
- 页面显示还是旧静态数据
- 接口成功但页面没刷新
- 页面跳转路径错误
- token 没有写入或没有带到请求头

### 更偏后端问题的典型表现

- 请求已发出但返回 401 / 403 / 404 / 500
- 返回结构和页面预期不一致
- 订单状态没有按业务动作变化
- `driverId` 没写入
- 司机端看不到自己应看到的订单

---

## 联调通过标准

- [ ] 用户可注册
- [ ] 用户可登录
- [ ] 用户可创建订单
- [ ] 客户端可看到 `pending`
- [ ] 司机端可看到该订单
- [ ] 司机可接单
- [ ] 客户端状态变为 `accepted`
- [ ] 司机可开始行程
- [ ] 客户端状态变为 `ongoing`
- [ ] 司机可完成订单
- [ ] 客户端状态变为 `completed`

